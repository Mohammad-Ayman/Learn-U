/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "assets.api.uizard.io",
      "www.nyip.edu",
      "plus.unsplash.com",
      "media.istockphoto.com",
    ],
  },
};

module.exports = nextConfig;
